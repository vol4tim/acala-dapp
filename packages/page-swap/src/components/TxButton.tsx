import { styled } from '@acala-dapp/ui-components';
import { TxButton } from '@acala-dapp/react-components';

export default styled(TxButton)`
  margin: 0 auto;
  padding: 0 40px;
  border-radius: 10px;
`;
